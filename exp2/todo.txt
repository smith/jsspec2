jsspec.dsl.BDD(function(_) {
	_.given('odd and even numbers');
		_('odd', 1);
		_('even', 1);
	
	_.when('add two numbers');
		_('added', _('odd') + _('even'));
	
	_.then('should be odd');
		_('added').should_be(3);
});